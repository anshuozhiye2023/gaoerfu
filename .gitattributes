# 所有文本文件在仓库里统一存 LF。
# 为什么：本机 Windows 的 core.autocrlf 会把 checkout 出来的文件转成 CRLF，
# 而 shell 脚本的 shebang（#!/usr/bin/env bash\r）和 CI 的 run 块在
# Linux/macOS 构建机上会因此报 "bad interpreter" / 命令尾部多个 \r。
* text=auto

*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# 二进制资源别动
*.png  binary
*.jpg  binary
*.jks  binary
*.keystore binary
